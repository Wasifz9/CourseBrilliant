export default function recommend(bpMap, cartedCourses){
    // // The code so far should give you all the unique semesters, course levels, and individual courses.
    
    // // 1. Loop through the data structures:

    // // cartcourses - array
    // // blueprint - map
    // var reference_courses = [];
    // var unique_semesters = [];

    // for (course of cartedCourses) {
    //     reference_courses.push(course)
    // }
    // for ([semester, course] of bpMap.entries()) {
    //     if (! reference_courses.includes(course)) {
    //         reference_courses.push(course)
    //     }
    //     if (! unique_semesters.includes(semester)) {
    //         unique_semesters.push(semester)
    //     }
    // }

    // // 2. We can group them based on their prereqs, postreqs, and
    // // the combination of (level and semester).
    // var course_level = [];

    // for (course of reference_courses) {
    //     var level = parseInt(course.charAt(3));
    //     if (! course_level.includes(level)) {
    //         course_level.push(level)
    //     }
    // }  
    
    return; 
}